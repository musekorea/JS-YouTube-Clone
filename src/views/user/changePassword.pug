extends ../layout.pug 

block content 
  form(method="post")
    input(placeholder="Old Password"  name="oldPwd")
    input(placeholder="New Password" name="newPwd1")
    input(placeholder="New Password Confirmation" name="newPwd2")
    input(type="submit" value="Change Password" )
  if errorMessage 
    h2 #{errorMessage}